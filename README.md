### Bonuses :

* Visual mode:
  -v, -v full
  colors
  warning about size of the terminal if too little
  player, vm and history display
  control speed of execution, stop and continue (exec frame per frame)
  death message (with bell) when a champions die
  victory message
  display player info in full mode with a bindkeys
